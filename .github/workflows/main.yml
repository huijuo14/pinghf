name: Ping Hugging Face Spaces

on:
  schedule:
    # Run every 30 minutes
    - cron: '*/30 * * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  ping-spaces:
    runs-on: ubuntu-latest
    steps:
      - name: Ping testspace2
        run: |
          echo "Pinging testspace2..."
          curl -s -o /dev/null -w "Status: %{http_code}\n" "https://huijio-testsurf2.hf.space" || true
          
      - name: Ping testspace3
        run: |
          echo "Pinging testspace3..."
          curl -s -o /dev/null -w "Status: %{http_code}\n" "https://huijio-testsurf3.hf.space" || true
          
      - name: Ping testspace4
        run: |
          echo "Pinging testspace4..."
          curl -s -o /dev/null -w "Status: %{http_code}\n" "https://huijio-testsurf3.hf.space" || true
          
      - name: Ping testspace5
        run: |
          echo "Pinging testspace5..."
          curl -s -o /dev/null -w "Status: %{http_code}\n" "https://huijio-testsurf5.hf.space" || true
          
      - name: Log completion
        run: echo "✅ All spaces pinged successfully at $(date)"
